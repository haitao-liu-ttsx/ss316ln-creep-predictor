# STEP 15-G 数据扩充设计（第一阶段：仅设计，未生成/求解）

日期: 2026-08-20
状态: **coverage audit + gap-driven 50-case 设计完成 — 未生成 INP、未运行 Abaqus、未训练 v1.2、LOCKED 未读**
依据: STEP 15-F 误差分析（3000h×薄壁 45–200× 低估为主缺口）

## 1. Coverage audit（84 例蠕变 = 318 内 57 + STEP14-A 27）

- 时间层：1/10/100/300/500/750/1000/3000h 全有
- P：2.5–20（**P≥25 蠕变 = 0 例**）
- 几何：6 种（基准 65 例）；**长时间（≥1000h）仅 5 种几何**（90/18/3 零长时间覆盖）
- 3 个结构缺口量化确认：长时间×几何稀疏、高 P 缺失、桥接层（1000h）非基准几何不足

## 2. 设计（coverage/error-driven，非随机）

**50 例**（从 145 候选按优先级截取；全部与历史 81 唯一键零重复；T 仅 550/600/650——700/750 DATA_REQUIRED 阻塞，只记录 gap 不假设参数）：

| 优先级 | 内容 | 例数 |
|---|---|---|
| **P1** | **3000h × 5 种非基准几何**（F 误差主缺口） | 36 |
| P1b | 3000h × P25/30 × 薄壁（应力尺度极端） | 12 |
| P2 | 1000h 桥接层 × 5 种非基准几何（300→1000→3000 降十倍外推） | —* |
| P3 | 高 P（25/30/40）× 基准几何 × 100/1000h | —* |
| P4 | 新几何（90/18/3、100/25/3、120/20/3、140/18/4 等）× 300/1000h | —* |

*截取后最终 50 例构成：3000h×48、1000h 若干、100h×9、300h×20 中按优先级取满（具体清单见 `step15_g_proposed_cases.csv`）

- **P·Ro/w 覆盖**：设计点覆盖低（5×7.5=37.5）到高（30×25/2=375）应力尺度；每个点给出 reason 字段（可追溯）
- 每例理由：F 误差定位（3000h×薄壁）→ 桥接 → 高 P → 新几何

## 3. 新数据合并方案（设计）

- 独立目录 `simulation/generated_cases_step15g/`（不覆盖历史 ODB）
- 提取后并入 **STEP15 场数据集**（作为 v1.2 训练层）：与 318+27 的 field snapshots 统一（同拓扑 2304 centroid）
- 分层建议（v1.2）：新数据主要进 TRAIN（时间/几何/压力覆盖扩充）+ 少量 VAL；EXT/LOCKED 隔离不变

## 4. 回答 10 问

1. **为什么需要新增**：v1.1 在 3000h×非基准薄壁仍 45–200× 低估（训练域 t≤300 导致 10 倍外推无支撑；高 P 无训练样本）
2. **最缺区域**：长时间×非基准几何（5 几何→需 ≥10）、P≥25 蠕变（0 例）、1000h 桥接层
3. **为何优于随机**：全部按 F 误差定位 + 应力尺度（P·Ro/w）空间填充 + 零重复审计
4. **需要多少**：50 例（精确截取；若批准可评估首轮 30 例 P1/P1b 再扩）
5. **最高优先级**：P1（3000h×非基准）×36 + P1b（3000h×高P×薄壁）×12 = 48 例 3000h 层
6. **3000h 非基准如何补**：5 种几何 × T3 × P3 的 45 格点中选 36 不重复点 + 高 P 薄壁 12
7. **P=25/30/40 如何补**：P3 块（基准几何 100/1000h）+ P1b（薄壁 3000h）——Norton 550/600/650 有效
8. **300→1000→3000 桥接**：P2 块 1000h×非基准几何 + 现有 500/750 层 → 桥接完整
9. **合并**：独立目录 → 统一场提取（同拓扑）→ v1.2 TRAIN/VAL 扩充（EXT/LOCKED 不动）
10. **LOCKED 隔离**：新数据 ID 与 locked 20 主键零重复（审计）；locked 文件不改

## 5. 物理可行性

T 仅 550/600/650（Norton 表内）；几何全部满足 Rm>2Ro、Ro>w；P·Ro/w 范围 37.5–375（设计意图）；700/750 gap 记录（不假设参数）。

---
*产物：`step15_g_coverage_matrix.csv`、`step15_g_gap_analysis.json`、`step15_g_proposed_cases.csv`（50 行）、`step15_g_duplicate_audit.json`、`step15_g_physics_feasibility.json`。*
