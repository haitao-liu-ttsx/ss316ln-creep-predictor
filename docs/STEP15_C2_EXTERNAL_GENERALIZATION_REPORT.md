# STEP 15-C.2 外部泛化测试报告（EXT 27 例，一次性）

日期: 2026-08-20
状态: **冻结→预测→首次读 target→评估→审计 全链完成 — 未重训、未改模型、未读 LOCKED**
声明: **"STEP15 field surrogate v1 external evaluation completed"**（非"已成功泛化"结论，见 §9）

## 1. 模型冻结（C.2.1）

- POD(log10 域, k=3) + XGBoost 系数回归器 ×3；9 特征、log1p_time、scaler train-only
- `STEP15_C2_FREEZE_MANIFEST.json`：模型文件 SHA256（pod_basis `201d7dff…`、model `00f6c085…`、config `289d4bb3…`）、train 31/val 6/ext 27/locked 20 IDs、dataset/locked checksum、freeze 时间戳
- 冻结后模型零修改

## 2. EXT 清单（C.2.2）

27 例（t500/750 基准 18 + t3000 非基准 9）；ID 唯一、与 train/val/locked 零重叠、ODB 27/27 存在可读；target_status 冻结前 = NOT_READ

## 3. 评估（C.2.5–2.9，首次读 target 于冻结后）

### Overall（27 例）
**logMAE=1.166 / logR²=0.673 / relL2=0.999 / hotspot_hit=1.00 / top5=0.84**；physics violations=0

### 时间分组（暴露核心缺陷）
| t | n | logMAE | logR² | 说明 |
|---|---|---|---|---|
| 500h | 9 | 0.475 | 0.963 | ✅ 好（基准几何） |
| 750h | 9 | 0.651 | 0.934 | ✅ 好 |
| **3000h** | 9 | **2.373** | **−0.004** | ❌ 失效 |

### 几何分组
| 几何 | n | logMAE | logR² | hotspot |
|---|---|---|---|---|
| (100,20,4) 基准 | 18 | 0.563 | 0.949 | 1.00 |
| (80,15,2) | 3 | 2.474 | 0.180 | 1.00 |
| (120,25,3) | 3 | 3.438 | −0.307 | 1.00 |
| (150,20,4) | 3 | 1.205 | −0.976 | 1.00 |

### Hotspot（最重要的正面结果）
**27/27 热点元素全部命中（element 336），top-5 重叠 0.84**——即使量级低估 100–2700×，**蠕变最剧烈的位置预测完全正确**。空间模式学习是成功的。

## 4. 核心科研结论（三能力验证）

1. **空间场结构**：✅ 学对了（热点 100%、top5 0.84——模式 φ(x) 正确）
2. **时间外推**：❌ **XGB 树模型常数外推失效**（t 超训练域 ≤300h 后树无分裂点 → t500 与 t750 预测完全相同；t=3000 量级低估 10–2700×）
3. **几何 domain shift**：❌ 量级放大未表达（与 STEP14 scalar 同根因：P·Ro/w 应力放大不在输入结构内；但**位置信息**是场模型新增价值）

## 5. 与 STEP14 scalar baseline 对比

| | STEP14 PhysB-quad（scalar max） | STEP15 v1（2304 场） |
|---|---|---|
| ext 9 例 logMAE | 1.221 | 1.166（27 例 overall，含 18 好例） |
| 时间外推 | ✅ 显式 log10(t) 项 | ❌ 树常数外推 |
| 几何放大 | ❌ 未表达（×47 低估） | ❌ 未表达（×100–2700） |
| **空间热点** | **无法提供** | **✅ 100% 命中** |

**回答核心问题**：STEP15 场模型**确实增加了空间预测能力**（热点/场结构），但**量级外推能力仍受限于与 STEP14 相同的结构性缺陷**（无显式时间/几何解析项）——XGB 回归器的形式限制了外推。

## 6. 根因与修复方向（STEP 15-D 建议，未执行）

1. **时间**：POD 系数对 t 的解析结构 c_i = g_i(T,P,geom) + δ_i·log10(t)（物理基线 A 的场级推广——CEEQ∝t 已知）→ 修复树模型常数外推
2. **几何**：输入中加入 P·Ro/w（或应力尺度特征）→ 表达几何放大（STEP13.7 已验证该特征对树模型有效）
3. 修复后重训需重新冻结（新 v1.1，非本 v1）

## 7. 数据保护与 audit

318 `20f21ebc67ea`、locked `fa573e330926` unchanged；LOCKED 未读；EXT target 仅在冻结 manifest 记录 NOT_READ 后首次读取（单向门）；无重训/无选择/无调参/POD 未重拟合。

---
*v1 外部泛化测试如实完成；失败维度未隐藏，作为 v1.1 改进依据。*
