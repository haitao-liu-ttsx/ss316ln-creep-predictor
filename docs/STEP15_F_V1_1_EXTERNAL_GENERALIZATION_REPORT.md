# STEP 15-F v1.1 外部泛化测试报告（EXT 27 例，一次性）

日期: 2026-08-20
状态: **v1.1 冻结 + EXT 一次性评估完成 — LOCKED 未读、无重训、无选择**
冻结: `ml/final/step15_v1_1/STEP15_F_FREEZE_MANIFEST.json`（模型/特征/k/时间结构/几何特征/checksum/时间戳，SHA256 锁定）

## 1. 冻结模型（Candidate D）

POD(log10, k=3, TRAIN-only) + XGB 残差回归器×3（TRAIN-only）+ 解析时间项 d_i·log10(t)（d=[32.56,−0.14,−0.22]）+ 特征 log10(P·Ro/w)。单向门：预测保存 → 才读 EXT target（v1.1 首次）。

## 2. v1 vs v1.1 对比（同一 EXT 27）

| 指标 | v1 | **v1.1** |
|---|---|---|
| **overall logMAE** | 1.166 | **0.591** |
| **overall logR²** | 0.673 | **0.871** |
| 500h logMAE | 0.475 | **0.155**（R²=0.996） |
| 750h logMAE | 0.651 | **0.212**（R²=0.993） |
| 3000h logMAE | 2.373 | **1.406**（R² **−0.004→0.575**） |
| 基准几何 logMAE | 0.563 | **0.184**（R²=0.995） |
| (80,15,2) | 2.47 | **1.45** |
| (120,25,3) | 3.44 | **2.39** |
| (150,20,4) | 1.21 | **0.384** |
| hotspot hit | 1.00 | **1.00**（27/27） |
| top5 | 0.84 | 0.59 |

## 3. 两个核心问题判定

**问题 A（时间外推）**：
- **500/750h：完全成功**（logR² 0.996/0.993）——v1 的树常数外推问题已消失（解析 log10(t) 项生效）
- **3000h：显著改善但未完全解决**（R² −0.004→0.575；9 例中 5 例良好 ratio 1.4–2.4/logMAE≤0.45，4 例**3000h×薄壁非基准**组合仍差 ratio 45–200×）

**问题 B（几何 domain shift）**：
- **显著改善**（各几何 logMAE 减半至 1/3；低估从 ×100–2700 收窄至 ×45–200）——log10(P·Ro/w) 提供了部分应力放大表达
- 残余缺口 = 3000h×薄壁（应力放大仍不完整）

## 4. Physics：0 violations（CEEQ≥0、finite、hotspot 空间一致 27/27）

## 5. 科研分层结论（按要求）

1. **v1.1 是否比 v1 好：是**（overall logMAE 减半、logR² 0.67→0.87；全部时间/几何分组改善）
2. **时间外推 500/750/3000h 是否成功**：500/750 **成功**；3000h **部分成功**（结构性外推正确——常数外推消失——但薄壁 3000h 量级仍差）
3. **Geometry shift 是否改善：是**（全面改善，残余在 3000h×薄壁）
4. **3000h 是否仍需新增 Abaqus 数据：是（明确）**——需要 1000/3000h 场训练/验证数据（尤其非基准几何×长时），当前训练域 t≤300h 使 3000h 是 10 倍外推
5. **Hotspot 是否保持可靠：是（27/27 全命中）**
6. **v1.1 是否可作为当前 production surrogate**：**有限域 production 可**（t≤750h、覆盖几何域 logR²≥0.99、热点可靠）；**3000h×薄壁域不可靠**——须明确标注边界
7. **是否需要 STEP 15-G 数据扩充/二次训练：建议**（3000h×非基准几何为主缺口；几何 6→10 种；P 25–40）

## 6. 数据保护

318 `20f21ebc67ea`、locked `fa573e330926` unchanged；LOCKED 未读；EXT target 仅冻结后一次性读取；无重训/无选择/无调参；v1 与 C.2 结果原样保留。

## 7. 状态声明

v1.1 model frozen = **YES** ｜ EXT 27 evaluated = **YES** ｜ EXT target read = **YES（仅冻结后一次）** ｜ LOCKED TEST read = **NO** ｜ 318 dataset modified = **NO** ｜ v1 modified = **NO** ｜ post-EXT retraining = **NO** ｜ post-EXT model selection = **NO**

---
*v1.1 有限域 production 候选（边界明确标注）；3000h×薄壁为 STEP 15-G 数据缺口。*
